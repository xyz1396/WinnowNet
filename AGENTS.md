# Environment

Use the `micromamba` environment `winnownet` for all Python-related commands.

Prefer:
`micromamba run -n winnownet <command>`

If an interactive shell is needed first, activate:
`micromamba activate winnownet`

Run `rg` as:
`micromamba run -n winnownet rg <args>`
